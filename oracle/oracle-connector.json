{
  "name": "t24-account-cdc",
  "config": {
    "connector.class": "io.debezium.connector.oracle.OracleConnector",
    "tasks.max": "1",

    "database.hostname": "oracle",
    "database.port": "1521",
    "database.user": "c##dbzuser",
    "database.password": "dbz",
    "database.dbname": "XE",
    "database.pdb.name": "XEPDB1",

    "topic.prefix": "t24",
    "table.include.list": "T24\\.ACCOUNT",

    "//": "Only the 2 physically stored columns - the other 9 are virtual and arrive as garbage text without this filter.",
    "column.include.list": "T24\\.ACCOUNT\\.(RECID|XMLRECORD)",

    "//lob": "Required - XMLRECORD is XMLTYPE, stored as a hidden BLOB.",
    "lob.enabled": "true",

    "//strategy": "online_catalog avoids waiting on log archival to see changes (default redo_log_catalog can stall indefinitely on XE's small redo logs). Trade-off: can't track DDL on the captured table - fine here.",
    "log.mining.strategy": "online_catalog",

    "schema.history.internal.kafka.bootstrap.servers": "kafka:9092",
    "schema.history.internal.kafka.topic": "schema-history.t24",

    "snapshot.mode": "initial",

    "key.converter": "org.apache.kafka.connect.json.JsonConverter",
    "key.converter.schemas.enable": "false",
    "value.converter": "org.apache.kafka.connect.json.JsonConverter",
    "value.converter.schemas.enable": "false"
  }
}
